class LearningModeData {
  Duration flashCardDuration;
  Duration quizDuration;
  Duration typeDuration;

  LearningModeData({
    this.flashCardDuration = Duration.zero,
    this.quizDuration = Duration.zero,
    this.typeDuration = Duration.zero,
  });
}